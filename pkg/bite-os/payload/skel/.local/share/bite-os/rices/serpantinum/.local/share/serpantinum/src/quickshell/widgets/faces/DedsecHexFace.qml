// ◈ BITE-OS addition to serpantinum (upstream AGPL-3.0, (C) Illia Miroshnichenko).
// Dedsec monitor, Hex look: the live readout encoded as a hex dump.
import QtQuick

DedsecFace {
    look: "hex"
}
