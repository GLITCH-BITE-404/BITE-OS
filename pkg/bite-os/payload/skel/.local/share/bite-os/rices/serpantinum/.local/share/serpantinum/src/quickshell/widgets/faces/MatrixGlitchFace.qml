// ◈ BITE-OS addition to serpantinum (upstream AGPL-3.0, (C) Illia Miroshnichenko).
// Matrix rain, Glitch look: katakana rain with RGB-split heads and columns that
// jolt sideways in red/blue for a few frames.
import QtQuick
import "../../"

MatrixFace {
    glitch: true
    cTrail: ThemeBackend.mauve
}
