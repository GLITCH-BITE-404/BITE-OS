// ◈ BITE-OS addition to serpantinum (upstream AGPL-3.0, (C) Illia Miroshnichenko).
// Matrix rain, Binary look: the same falling columns, 0s and 1s only.
import QtQuick
import "../../"

MatrixFace {
    glyphs: "01"
    glyphFont: ThemeBackend.fontFamily
}
